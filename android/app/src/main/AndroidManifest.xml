<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tradingmonitor">
    <application
        android:name=".MainApplication"
        android:label="Trading Monitor"
        android:icon="@mipmap/ic_launcher">
        <meta-data
            android:name="com.google.firebase.database.database_url"
            android:value="https://tradingmonitor-mt5-default-rtdb.europe-west1.firebasedatabase.app/" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>