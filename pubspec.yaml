name: exi_trading_monitor
description: Firebase-based MT5 trading monitor
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_database: ^10.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
